export const DATABASE_HASH_PREFIX = `#database_`

export const DatabaseTabs = {
  Actors: `${DATABASE_HASH_PREFIX}actors`,
  // Classes: `${DATABASE_HASH_PREFIX}classes`,
  Animations: `${DATABASE_HASH_PREFIX}animations`,
}
